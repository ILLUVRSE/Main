# kernel/.secrets.example
#
# DO NOT COMMIT REAL SECRETS. This file documents the names and example formats
# of secrets that must be provided via your host's secret manager (Fly/Render),
# or via GitHub Actions repository/org secrets for CI.
#
# Save-on-host: never commit real values into the repository. Use the platform UI
# or CLI to inject values at deploy/runtime.

# REQUIRED - Database
# Example: postgresql://user:password@db.host:5432/illuvrse?sslmode=require
POSTGRES_URL="postgresql://postgres:REPLACE_ME@db.example.com:5432/illuvrse?sslmode=require"

# REQUIRED - KMS / Signing proxy
# Example: https://kms.internal/sign
KMS_ENDPOINT="https://kms.example.internal"

# REQUIRED - signer identifier known to KMS
SIGNER_ID="kernel-signer-1"

# REQUIRED for production: set to 'true' in CI to enforce KMS
REQUIRE_KMS="true"

# OPTIONAL - Container registry credentials (if CI pushes images)
DOCKERHUB_USERNAME="your-dockerhub-username"
DOCKERHUB_PASSWORD="xxxxxxxxxxxxxxxx"
DOCKERHUB_REPO="yourorg/illuvrse-kernel"

# OPTIONAL - Fly.io / cloud provider deploy token (if CI deploys)
FLY_API_TOKEN="FLY_API_TOKEN_REPLACE_ME"

# OPTIONAL - Observability / error reporting
SENTRY_DSN="https://public@sentry.io/12345"

# OPTIONAL - Prometheus pushgateway (for metrics)
PROMETHEUS_PUSHGATEWAY_URL="https://pushgateway.example"
PROMETHEUS_JOB_NAME="kernel"

# NOTES / commands (examples)
# - GitHub Actions secret:
#   Settings -> Secrets and variables -> Actions -> New repository secret
#   Name: POSTGRES_URL  Value: postgresql://...
#
# - Fly (example):
#   flyctl secrets set POSTGRES_URL="..." KMS_ENDPOINT="..." SIGNER_ID="kernel-signer-1"
#
# - Render / DigitalOcean / Render UI: add environment secrets in the service settings.
#
# - For CI, do NOT echo secrets into logs. Use secrets directly in environment bindings.
#
# Acceptance:
# - All required secrets documented above are present in the target host/CI secret manager before production deploy.
# - No real secret values exist in repository history or commits.

