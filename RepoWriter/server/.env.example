# RepoWriter server environment example
# Copy this to RepoWriter/server/.env and fill in real values (do NOT commit .env).

# OpenAI credentials
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY
OPENAI_PROJECT_ID=proj-OPTIONAL_PROJECT_ID_OR_EMPTY

# Optional: override OpenAI base URL (useful for tests with openaiMock)
# Example: http://localhost:9876
# If empty, client will call https://api.openai.com
OPENAI_API_URL=

# Repo config
# Absolute path to the repository root that RepoWriter may edit (defaults to process.cwd())
REPO_PATH=/absolute/path/to/ILLUVRSE/Main

# Server
PORT=7071

# Git defaults used when creating commits
GITHUB_REMOTE=origin
GIT_USER_NAME=RepoWriter Bot
GIT_USER_EMAIL=repowriter@example.com

# Optional rate limit tuning
# window in ms and max requests per window
REPOWRITER_RATE_LIMIT_WINDOW_MS=60000
REPOWRITER_RATE_LIMIT_MAX=60

# Sandbox options (used by sandboxRunner)
SANDBOX_TIMEOUT_MS=30000
SANDBOX_KEEP=false

# Notes:
# - Make sure RepoWriter/server/.env is listed in .gitignore (do not commit secrets).
# - If you use an OpenAI mock for tests, set OPENAI_API_URL to the mock base URL and the code will use it when present.

